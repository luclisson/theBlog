help to improve developer productivities.

to make use of them you need to add them to the pom.xml dependencies
```
<artifactId>spring-boot-devtools</artifactId>
```

with this dependencies added e.g. your server is restarted automatically on safe. This improves testablity insaneeeeee.
im sure there is more maybe add a page about a documentation